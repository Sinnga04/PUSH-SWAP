indexing each node ( of both stacks)

analyze how much each node takes to go to the top -> cost

figure out what iti is his target node (the number in the other stack that is bigger and the most 
nearest otherwise the smallest)

cheapest 

add the cost of each node and his target node ! --≥ move the smallest 


